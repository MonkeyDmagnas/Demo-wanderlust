name: CICD pipeline for HIDR

on:
  push:
    branches:
      - dev
      - staging

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install and build
        script: |
            cd frontend
            npm install
            npm run build